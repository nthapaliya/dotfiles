[user]
	name = Niraj Thapaliya
	email = nthapaliya@users.noreply.github.com
[push]
	default = simple
	followTags = true
[core]
	ignorecase = false
	quotepath = false
	pager = delta
[interactive]
	diffFilter = delta --color-only
[alias]
	aliases = !git config --get-regexp 'alias.*' | colrm 1 6 | sed 's/[ ]/ = /' | sort
	assume = update-index --assume-unchanged
	assumed = !git ls-files -v | grep ^h | cut -c 3-
	conflict = diff --name-only --diff-filter=U
	ctags = !.git/hooks/ctags-run
	patch = !git --no-pager diff --no-color
	publish = !git push -u origin $(git branch --show-current)
	purge = "!f() { local gcb=$(git branch --show-current) && git branch --merged \"$gcb\" | grep -v master | grep -v \"$gcb\" | awk '{ print $NF }' | xargs git branch -d ; } ; f"
	release-branch = !git branch -r | grep 'origin/v3\\...\\.x$' | grep -o 'v3\\...\\.x$' | sort -V | tail -1
	unassume = update-index --no-assume-unchanged
	unique = log HEAD --not master
	up = "!git remote update -p; git merge --ff-only @{u}"
	who = "!f() { git log --format=%an \"$@\" | sort | uniq -c | sort -rn ; }; f"
[init]
	templatedir = ~/.config/git/template
[status]
	showUntrackedFiles = all
[stash]
	showPatch = true
[diff]
	indentHeuristic = on
	renamelimit = 10000
[commit]
	verbose = true
[rebase]
	autosquash = true
[color]
	ui = true
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 22
[color "diff"]
	meta = yellow
	frag = magenta bold
	commit = yellow bold
	old = red bold
	new = green bold
	whitespace = red reverse
[protocol]
	version = 2
[log]
	decorate = short
	abbrevCommit = true
