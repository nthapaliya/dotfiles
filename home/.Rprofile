options(repos = c(CRAN = sprintf("https://packagemanager.posit.co/cran/latest/bin/linux/rhel10-%s/%s",
                                 R.version["arch"],
                                 substr(getRversion(), 1, 3))))
# Sys.setenv(PATH=paste(file.path(Sys.getenv("HOME"), '.local/bin'), Sys.getenv("PATH"), sep=':'))
