#!/usr/bin/env Rscript

args <- commandArgs(trailingOnly = TRUE)

if (length(args) != 1) {
  stop("Must supply exactly one argument!")
}

if (!file.exists(args[1])) {
  stop(paste("Not found", args[1]))
}

rmarkdown::render(args[1], "pdf_document")
